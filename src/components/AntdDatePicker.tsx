import { DatePicker, DatePickerProps } from 'antd'

const AntdDatePicker = (props: DatePickerProps) => {
  return <DatePicker className='antd-date-picker' {...props} />
}

export default AntdDatePicker
